// TODO: Render the `App` component to the DOM

// to get the watch working I ran this
/*

babel . --babel-watch --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled --source-maps inline --watch

*/

ReactDOM.render(<App />, document.getElementById("app"));


